{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Build",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}",
      "command": "/build/luajit -l modules.set-paths ${workspaceFolder}/build/scripts/ygofab.lua ${workspaceFolder} compose"
    }
  ]
}
